import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appli_jeuxvideo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
